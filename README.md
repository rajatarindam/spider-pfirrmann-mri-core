## Running Modules

Always execute project modules from the project root using:

python -m <module>

Examples:

python -m eda.dataset_inventory
python -m eda.slice_statistics
python -m preprocessing.unpack_dataset